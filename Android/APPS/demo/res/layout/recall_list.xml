<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="fill_parent"
    android:background="#e3000000"
    android:orientation="vertical"
    android:padding="10dp" >

    <EditText
        android:id="@+id/editText1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:background="@drawable/clicked"
        android:drawableLeft="@android:drawable/ic_menu_search"
        android:drawablePadding="5dp"
        android:ems="10"
        android:hint="Search By Quote#"
        android:padding="6dp"
        android:visibility="gone" />

    <ListView
        android:id="@+id/recall_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/close_recall"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/editText1" >

    </ListView>

    <Button
        android:id="@+id/close_recall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:minWidth="90dp"
        android:text="CANCEL" />

    <ProgressBar
        android:id="@+id/recall_pr"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true" />

</RelativeLayout>